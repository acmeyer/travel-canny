.authform.mx-auto
  = render 'registration_steps/registration_wizard_steps', step: 'trip'
  .card
    .card-body
      -# = form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :role => 'form'}) do |f|
      -#   %p.lead.text-center= t('views.registration.add_trip')
      -#   %hr
      -#   = devise_error_messages!
      -#   .form-group
      -#     = f.email_field :email, class: 'form-control', placeholder: t('forms.email')
      -#   .form-group
      -#     = f.password_field :password, class: 'form-control', placeholder: t('forms.password')
      -#   .form-group
      -#     = f.password_field :password_confirmation, class: 'form-control', placeholder: t('forms.confirm_password')
      -#   = f.submit t('buttons.next'), :class => 'btn btn-primary btn-block', data: { disable_with: t('buttons.loading') }
      